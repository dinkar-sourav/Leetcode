#include <bits/stdc++.h>
using namespace std;

void print(vector<int> &v){
    for(auto it:v){
        cout<<it;
    }
}
int main(){
   string s;
   getline(cin,s);
   stringstream sobj(s);
   int num;
   vector<int> v;
   while(sobj>>num){
        v.push_back(num);
   }

   print(v);
}
